source_up

export ANTHROPIC_API_KEY=$(op item get co34pzkizfie5ax2joyfz4754u \
    --vault cli \
    --account my.1password.com \
    --fields label=ANTHROPIC_API_KEY \
    --reveal 2>/dev/null)

# Suppress noisy BAML runtime logs; set to "info" to see request traces.
export BAML_LOG=error
