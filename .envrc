source_up

# Secrets are inherited from the global loader in /Users/joe/dev/.env.nu.
# Default provider is set in .looprs/provider.json.

# Suppress noisy BAML runtime logs; set to "info" to see request traces.
export BAML_LOG=error

# Prefer gh keyring/SSH auth over any inherited env token.
unset GITHUB_TOKEN
