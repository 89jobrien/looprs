[profile.default]
retries = 0
fail-fast = false
test-threads = "num-cpus"

[profile.ci]
retries = 0
fail-fast = false
test-threads = 4

[[profile.default.overrides]]
filter = 'test(live_)'
retries = 0  # Don't retry live LLM tests
