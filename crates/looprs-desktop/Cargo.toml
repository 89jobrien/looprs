[package]
name = "looprs-desktop"
version = "0.1.0"
edition = "2024"

[dependencies]
freya = { version = "0.3.0", default-features = false, features = ["custom-tokio-rt", "mocked-engine-development"] }
tokio = { version = "1.38", features = ["macros", "rt-multi-thread"] }
looprs = { path = "../.." }
